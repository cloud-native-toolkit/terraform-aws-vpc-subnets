name: "aws-vpc-subnets"
type: terraform
description: "Create subnets for VPC"
tags:
    - infrastructure
    - networking
versions:
- platforms: []
  providers:
   - name: aws
     source: "hashicorp/aws"
  dependencies: []  
  variables:
    - name: region
      scope: global
