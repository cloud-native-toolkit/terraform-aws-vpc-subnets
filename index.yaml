id: github.com/cloud-native-toolkit/terraform-aws-vpc-subnets
name: "aws-vpc-subnets"
type: terraform
description: "Create subnets for VPC"
tags:
  - infrastructure
  - networking
versions: [{platforms: [], providers: [{name: aws, source: "hashicorp/aws"}], dependencies: [], variables: [{name: region, scope: global}, {name: prefix_name, type: string, description: Prefix to be added to the names of resources which are being provisioned, default: swe, optional: true}, {name: provision_igw, type: bool, description: Provision Internet Gateway based if true., default: true, optional: true}, {name: igw_id, type: string, description: Id of the internet gateway alread associated with VPC., default: 'variable igw_id { type = string description = Id of the internet gateway alread associated with VPC. default = } /* Resource level', optional: true}, {name: variables */~, type: string}, {name: vpc_id, type: string, description: (Required) The VPC ID., default: "", optional: true}, {name: private_subnet_cidr, type: list(string), description: (Required) The CIDR block for the private subnet., default: '[10.0.125.0/24]', optional: true}, {name: public_subnet_cidr, type: list(string), description: (Required) The CIDR block for the public subnet., default: [], optional: true}, {name: availability_zones, type: list(string), description: List of availability zone ids, default: [], optional: true}, {name: customer_owned_ipv4_pool, type: string, description: 'Type of the subnet: Public / Private', default: "", optional: true}, {name: map_customer_owned_ip_on_launch, type: string, description: Specify true to indicate that network interfaces created in the subnet should be assigned a customer owned IP address. The customer_owned_ipv4_pool and  arguments must be specified when set to true. Default is false., default: "false", optional: true}, {name: map_public_ip_on_launch, type: string, description: (Optional) Specify true to indicate that instances launched into the subnet should be assigned a public IP address. Default is fals, default: "false", optional: true}, {name: tags, type: map(string), description: '(Optional) A map of tags to assign to the resource. If configured with a provider default_tags configuration block present, tags with matching keys will overwrite those defined at the provider-level.', default: '{ project = swe }', optional: true}, {name: public_subnet_tags, type: map(string), description: Tags for public subnets, default: '{ tier = public }', optional: true}, {name: private_subnet_tags, type: map(string), description: Tags for private subnets, default: '{ tier = private }', optional: true}, {name: public_subnets, type: list(string), description: List of Public subnets in VPC, default: [], optional: true}, {name: private_subnets, type: list(string), description: List of Private subnets in VPC, default: [], optional: true}, {name: allocation_id, type: string, description: (Optional) The Allocation ID of the Elastic IP address for the gateway. Required for connectivity_type of public., default: "", optional: true}, {name: connectivity_type, type: string, description: (Optional) Connectivity type for the gateway. Valid values are private and public. Defaults to public., default: public, optional: true}, {name: provision_ngw, type: bool, description: 'Flag to determise whether to provision NAT gateway. ', default: true, optional: true}, {name: acl_rules_pub_in, type: list(map(string)), default: [], optional: true}, {name: acl_rules_pub_out, type: list(map(string)), default: [], optional: true}, {name: acl_rules_pri_in, type: list(map(string)), description: Private subnets inbound network ACLs, default: [], optional: true}, {name: acl_rules_pri_out, type: list(map(string)), default: [], optional: true}], version: v1.0.0, outputs: [{name: vpc_id, description: 'output "vpc_id" {   description = "Description of my'}, {name: 'output"   value       = "value" }', description: 'output"   value       = "value" }'}, {name: public_subnet_ids, description: 'output "public_subnet_ids" {    value = data.aws_subnets.public_subnet_ids.ids   }'}, {name: private_subnet_ids, description: 'output "private_subnet_ids" {   value = data.aws_subnets.private_subnet_ids.ids }'}, {name: subnet_count_public, description: 'output "subnet_count_public" { value = local.num_of_public_sn_cidrs }'}, {name: subnet_count_private, description: 'output "subnet_count_private" { value = local.num_of_private_sn_cidrs }'}]}]
